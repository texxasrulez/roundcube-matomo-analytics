<?php
// Base URL to your Matomo instance (no trailing slash), e.g. https://analytics.example.com
$config['matomo_analytics_url'] = '';

// Your Matomo Site ID (integer)
$config['matomo_analytics_site_id'] = 0;

// Respect browser Do Not Track header
$config['matomo_analytics_respect_dnt'] = true;

// If true, do not track authenticated Roundcube users (privacy-friendly)
$config['matomo_analytics_disable_for_authenticated'] = false;


// --- Debugging ---
// Enable verbose logging to Roundcube logs/matomo_analytics (or custom channel below)
$config['matomo_analytics_debug_enabled'] = false;

// Append an HTML comment after injection for easy view-source verification
$config['matomo_analytics_debug_html_comments'] = false;

// Optional: override the log channel name (defaults to 'matomo_analytics')
$config['matomo_analytics_debug_log_channel'] = 'matomo_analytics';


// --- Optional cookie/cross-domain settings ---
// If tracking across subdomains, set the cookie domain (e.g., '.example.com')
$config['matomo_analytics_cookie_domain'] = '';

// If tracking across multiple domains, list them (e.g., ['*.example.com', '*.example.org'])
$config['matomo_analytics_domains'] = array();

// Enable Matomo heartbeat timer (better time-on-page accuracy)
$config['matomo_analytics_enable_heartbeat'] = false;

// --- Debugging ---
// Enable verbose logging to Roundcube logs/matomo_analytics (or custom channel below)
$config['matomo_analytics_debug_enabled'] = false;

// Append an HTML comment after injection for easy view-source verification
$config['matomo_analytics_debug_html_comments'] = false;

// Log to browser console as well
$config['matomo_analytics_debug_console'] = false;

// Optional: override the log channel name (defaults to 'matomo_analytics')
$config['matomo_analytics_debug_log_channel'] = 'matomo_analytics';

// --- Fallback ---
// If true, inject a <noscript> image beacon to record pageviews even if JS is blocked
$config['matomo_analytics_fallback_img_beacon'] = false;
